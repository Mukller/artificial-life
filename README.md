# artificial-life

Цифровая жизнь в 300 строках Python.

Клетки едят, делятся, умирают. Эволюция происходит сама собой — никакого
умного кода, только простые правила и естественный отбор.

## Что внутри

- **Среда** — сетка с едой. Еда растёт случайно.
- **Организмы** — у каждого энергия, геном (скорость/размер/восприятие) и возраст.
- **Эволюция** — при делении геном мутирует. Слабые дохнут быстрее.
- **Визуализация** — ASCII в терминал или pygame окно.

## Запуск

```bash
pip install pygame
python life.py          # pygame окно
python life.py --ascii  # прямо в терминале
```

Параметры:
```
--width   ширина сетки (по умолчанию 80)
--height  высота сетки (по умолчанию 40)
--seed    начальное количество организмов
--fps     кадров в секунду
```

## Что наблюдать

Запусти и подожди минут 5. Посмотри как:
- популяция сначала взрывается, потом схлопывается
- выживают те, у кого баланс скорости и восприятия оптимален
- иногда возникают доминантные клоны

## Как устроен геном

```python
genome = {
    "speed":      float,  # сколько клеток проходит за ход
    "perception": float,  # радиус "видения" еды
    "size":       float,  # влияет на потребление энергии
    "mutation":   float,  # вероятность мутации потомка
}
```

Каждый параметр — компромисс. Быстрый жрёт больше энергии. Большой видит
дальше, но медленнее.

## Источник вдохновения

[artificial-life](https://github.com/MichaelMure/artificial-life) из HelloGitHub #121
